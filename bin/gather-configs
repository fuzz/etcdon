#!/bin/sh
#

p="${DON_PATH:-$HOME/.config/etcdon}"

echo -n "Gathering config files..."

if rsync -ac --files-from="$p"/etc/gathered-configs \
  root@"$DON_HOST":/ "$p"/local/configs; then
    echo "[OK]"; exit 0
else
    echo "[FAIL]"; exit 1
fi
